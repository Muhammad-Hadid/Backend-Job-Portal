# Environment Variables (keep .env in git for deployment)
# Note: Make sure to change sensitive values before deploying
.env.local
.env.development
.env.production
.env.example

# Uploaded Files (resumes, documents, etc.)
uploads/
!uploads/.gitkeep

# Node Modules
node_modules/

# Logs
*.log
npm-debug.log*
